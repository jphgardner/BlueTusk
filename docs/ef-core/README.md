# Entity Framework Core

EF Core provider implementation begins after the production ADO.NET baseline and type-system foundations. The project exists now to reserve package boundaries; it does not register an EF provider yet.

