# Extension SDK

Extensions register types and features through `BlueTusk.Extensions.Abstractions`. Optional packages must remain independently deployable and must use the same codec registry as built-in types.

