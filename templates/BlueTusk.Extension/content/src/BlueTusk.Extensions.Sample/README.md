# BlueTusk sample_type extension

```csharp
await using var dataSource = new BlueTuskDataSourceBuilder(connectionString)
    .UseSample()
    .Build();
```

Replace this text with the PostgreSQL extension prerequisites, supported server
versions, CLR semantics, SQL behaviour, and migration/EF integration status.
